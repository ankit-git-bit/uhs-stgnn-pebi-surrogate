# Figure Audit Table

This document summarizes the audit of all 12 figures in the UHS reservoir surrogate paper. Each figure has been validated to ensure the image content, caption text, in-text citations, and figure numbers are fully aligned.

| Figure Number | Image File | Caption | Referenced Section | Status |
| :--- | :--- | :--- | :--- | :--- |
| **Figure 1** | `figures/fig1_dataset_overview.png` | Overview of the Underground Hydrogen Storage (UHS) dataset, showing typical cyclic well operating schedules (injection/production BHP and flow rates) and the resulting average reservoir pressure over a 730-day (2-year) timeline. | Section 3 (`dataset.tex`) | **Resolved**. Verified that the image displays the dynamic scheduling and pressure timeline, matching the caption. In-text references are consistent. |
| **Figure 2** | `figures/fig2_mesh_topology.png` | Centroid coordinate distribution of the native 10,116-cell irregular PEBI/Voronoi mesh, highlighting the locations of the injector (red triangles) and producer (blue triangles) well centroids. | Section 3 (`dataset.tex`) | **Resolved**. Verified coordinate layout matches PEBI description and well centroid positions. |
| **Figure 3** | `figures/fig3_pressure_evolution.png` | Dynamic spatial pressure snapshots (bar) at representative timesteps during injection, shut-in, and production phases of Case 1, highlighting localized transient pressure gradients near the wellbores. | Section 3 (`dataset.tex`) | **Resolved**. Matches the spatial pressure map and highlights wellbore drawdown/buildup cones. |
| **Figure 4** | `figures/fig4_baseline_comparison.png` | Forecasting RMSE (bar) comparison on Test Case 5 for the direct pressure ($P_{t+1}$) and pressure-change ($\Delta P$) targets across baseline models. | Section 4 (`baseline_models.tex`), Section 5 (`pressure_change.tex`) | **Resolved**. Confirmed the bar chart shows the error comparisons between the two formulation targets. |
| **Figure 5** | `figures/fig5_feature_importance.png` | Gini feature importance ranking from the Random Forest model trained on the spatiotemporal feature set, showing the dominance of temporal lag ($\Delta P_{t-1}$) and time elapsed. | Section 6 (`feature_engineering.tex`) | **Resolved**. The chart correctly illustrates the importances (64.65% for single-step pressure lag $\Delta P_{t-1}$). |
| **Figure 6** | `figures/fig6_interpolation_error.png` | Pressure snapshot reconstruction RMSE (bar) comparing structured Cartesian grids ($64 \times 32$, $128 \times 64$, and $256 \times 128$) against the native 10,116-cell PEBI mesh, showing concentrated errors near wellbores. | Section 7 (`graph_surrogate.tex`) | **Resolved**. Verified the bar chart displays reconstruction RMSE and errors near the injector and producer. |
| **Figure 7** | `figures/fig7_stgnn_architecture.png` | Architecture of the Spatio-Temporal Graph Neural Network (ST-GNN), showing the integration of node/edge inputs, stacked transmissibility-weighted graph convolutions with residual skip connections and LayerNorm, and the GRU block for recurrent temporal update. | Section 8 (`stgnn.tex`) | **Resolved**. Professional block schematic representing the ST-GNN model architecture. |
| **Figure 8** | `figures/fig8_training_curves.png` | Training and validation loss curves (weighted MSE) over epochs for the ST-GNN configurations, showing stable convergence profiles. | Section 9 (`rollout_results.tex`) | **Resolved**. Confirmed curves show training/validation loss history over epochs for v2 and v3. |
| **Figure 9** | `figures/fig9_rollout_rmse.png` | Autoregressive rollout RMSE over the 142-step forecasting horizon on Test Case 5. Left: Pressure RMSE (bar). Right: Gas saturation ($S_g$) RMSE. | Section 9 (`rollout_results.tex`) | **Resolved**. Confirmed the dual-panel line plot maps error accumulation over the 142-step horizon. |
| **Figure 10** | `figures/fig10_pressure_tracking.png` | Pressure tracking comparisons between numerical reservoir simulator ground truth (solid lines) and ST-GNN v2 predictions (dashed lines) at select grid locations over the rollout. | Section 9 (`rollout_results.tex`) | **Resolved**. Inconsistency fixed. The plot title has been updated to "Pressure Field Tracking: ST-GNN v2 vs Reservoir Simulator" (previously incorrectly said v3) and the data represents ST-GNN v2, aligning fully with the caption and text discussions. |
| **Figure 11** | `figures/fig11_saturation_tracking.png` | Gas saturation ($S_g$) tracking comparisons between numerical reservoir simulator ground truth (solid lines) and ST-GNN v2 predictions (dashed lines) at select grid locations over the rollout. | Section 9 (`rollout_results.tex`) | **Resolved**. Inconsistency fixed. The plot title has been updated to "Gas Saturation Tracking: ST-GNN v2 vs Reservoir Simulator" (previously incorrectly said v3) and the data represents ST-GNN v2, aligning fully with the caption and text discussions. |
| **Figure 12** | `figures/fig12_bar_comparison.png` | Multi-step rollout comparison bar charts showing pressure and saturation errors at discrete rollout steps (t = 1, 10, 50, and 146) for ST-GNN v1, v2, and v3. | Section 9 (`rollout_results.tex`) | **Resolved**. Confirmed that the bar charts show discrete error states comparing v1, v2, and v3. |
